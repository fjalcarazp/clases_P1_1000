// BOM (Browser Object Model)

alert(navigator.platform); // Devuelve la plataforma en la que se ejecuta el navegador

// alert(navigator.platform); // Toda la información del propio navegador.

