// node.append(...nodes or strings) // agrega al final del elemento
// node.prepend(...nodes or strings) // agrega al principio del elemento
// node.before(...nodes or strings) // te lo agrega por fuera del elemento antes del elemento
// node.after(...nodes or strings) // te lo agrega por fuera del elemento después del elemento
// node.replaceWith(...nodes or strings) // te lo cambia por otro elemento

// insertAdjacentText
// insertAdjacentHTML
// remove()
// cloneNode(true)
// DocumentFragment()

